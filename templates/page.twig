{# TEMPLATE: DEFAULT PAGE #}

{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper ">


		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
			<section class="article-content">
				<h1 class="article-h1 font-sans text-white text-center text-2xl font-bold">{{post.title}}</h1>
				<div class="article-body text-white font-sans space-y-4 ml-5 mr-5">
					{{post.content}}
				</div>
			</section>
		</article>
	</div>

	

	<!-- /content-wrapper -->
{% endblock %}
